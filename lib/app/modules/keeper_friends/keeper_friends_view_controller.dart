import 'package:get/get.dart';

class KeeperFriendsViewController extends GetxController {
  // https://pub.dev/packages/modal_bottom_sheet
  // 1. 친구 목록을 다 가져오기
  // 2. 친구 목록의 각 친구 체크박스가 눌릴 때마다 `selectedFriendsList`를 update(push)
}
